<?
return array(
    'smart_filter_template' => array(
        'value' => '{h1} . {section.name}. {smartfilter.params.name} - {smartfilter.params.value} Цена - {smartfilter.price}',
        'readonly' => true,
    )
);